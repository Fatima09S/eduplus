spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eduplus_db
    username: postgres
    password: tableDev

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB

app:
  jwt:
    secret: eduplusSecretKey2026IPDDakarProjetL3Informatique
    expiration: 86400000
  upload:
    dir: uploads/

server:
  port: 8080